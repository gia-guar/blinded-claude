# MCP server + everything needed to actually run the dummy pipeline.
# In Phase 2 this is installed on top of the editable kedro project; the
# kedro/pandas/sklearn pins below simply confirm the runtime deps are present.
mcp>=1.9.0
kedro>=0.19.9,<0.20.0
kedro-datasets>=3.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
